# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'WallRay' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for WallRay
   pod 'Parse'
  target 'WallRayTests' do
    inherit! :search_paths
    # Pods for testing
   pod 'Parse'
  end

  target 'WallRayUITests' do
    inherit! :search_paths
    # Pods for testing
   pod 'Parse'
  end

end
